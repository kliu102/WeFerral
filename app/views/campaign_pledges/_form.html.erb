<%= form_for(@campaign_pledge) do |f| %>
  <% if @campaign_pledge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign_pledge.errors.count, "error") %> prohibited this campaign_pledge from being saved:</h2>

      <ul>
      <% @campaign_pledge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
